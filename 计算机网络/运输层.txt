通讯实体是主机中的进程 
IP协议可以传送到主机，但是不知道交付给哪个进程

运输层：复用与分用 端对端的逻辑通信
网络层： 主机对主机的逻辑通信

进行差错检测
面向连接的TCP和无连接的UDP

每个层对上次都是屏蔽的 透明的（类似于黑盒，模糊了内部实现）

UDP：用户数据报协议 使用之前不需要建立连接 收到之后不需要进行确定回复
TCP：传输控制协议   建立连接，之后释放连接

TCP报文段、UDP用户数据报

常用的应用层协议及其对应的运输层协议:
	DNS TFTP RIP DHCP SNMP NFS  IFMP  ----------- udp
	smtp telnet http ftp --------------tcp

pid 是变化的、

使用协议端口号：protocol port number 


应用层各种协议进程与运输实体进行层间交互的

tcp/ip 使用一个16位端口号，也就是说最大支持的端口为65535

常用的端口:
	21 ftp 23 telnet 25 smtp 53 dns 69 tftp snmp 161 snmp(trap) 162
系统端口号0-1023

www.iana.org
登记端口号
客户端使用的端口号（短暂端口号） 49152-max